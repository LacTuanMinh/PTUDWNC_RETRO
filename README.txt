API (firebase functions version): đã init theo dạng function. Lần sau chỉ cần chạy firebase deploy để upddate
				  lưu ý: sử dụng bcryptjs chứ ko phải bcrypt, nếu ko thì bị 'firebase function failed on loading user code'

(update): Api (heroku version) có thêm chức năng realtime dc deploy trên heroku


App: đã init theo dạng hosting, trước khi deploy phải npm run build tại root
Link: https://retro-minh-fit.web.app/
